const fs = require('fs'); const html = fs.readFileSync('index.html', 'utf8'); let newHtml = html.replace('<body class="bg-slate-950 font-sans text-slate-100 flex flex-col overflow-hidden select-none h-full w-full" dir="rtl">', '<body class="bg-slate-50 font-sans text-slate-900 flex flex-col overflow-hidden select-none h-full w-full" dir="rtl">'); const startReturn = html.indexOf('            return (\n                <div className="w-full h-full bg-slate-950'); const endReturn = html.indexOf('            );', startReturn) + 14; const newRet = fs.readFileSync('tmp_return.txt', 'utf8'); if(startReturn>-1){newHtml = newHtml.substring(0,startReturn) + newRet + newHtml.substring(endReturn);} fs.writeFileSync('index.html', newHtml);